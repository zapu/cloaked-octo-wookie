cradle = require 'cradle'

db = new (cradle.Connection)().database('market')

db.checkExists = (autocb) ->
	await @exists defer err, exists
	if err
		console.log 'CouchDB error', err
		process.exit 0

	unless exists
		@create()

module.exports = db