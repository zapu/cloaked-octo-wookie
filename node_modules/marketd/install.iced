db = require './couch'
await db.checkExists defer()

design = require './design'

###
console.log JSON.stringify design, (key, val) ->
	if typeof val == 'function' 
		val.toString() 
	else 
		val
###

await db.save design, defer err, res
if (err) 
	console.log 'Couch error', err

console.log 'Design document save result:', res